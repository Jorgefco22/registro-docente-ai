import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.registrodocenteai.app',
  appName: 'Registro Docente AI',
  webDir: 'dist'
};

export default config;
